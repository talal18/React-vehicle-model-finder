import React from "react";

const Title = props => (
  <div className="title-container__title-container">
    <h1 className="title-container__title">CAR MODEL FINDER</h1>
    <p className="title-container__subtitle">FIND ANY CAR BRAND MODELS</p>
  </div>
);

export default Title;
