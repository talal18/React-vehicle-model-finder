import React from "react";

const Title = props => (
  <div>
    <h1 className="title-container__title">CAR MODEL FINDER</h1>
    <p className="title-container__title">FIND ANY CAR BRAND MODELS</p>
  </div>
);

export default Title;
